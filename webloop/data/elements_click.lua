local ElementClick = {
    body = require("elements.body").onclick,
    text = require("elements.body").onclick,
    div = require("elements.body").onclick,
    link = require("elements.link").onClick,
}

return ElementClick