local ElementSize = {
    body = require("elements.body").size,
    text = require("elements.text").size,
    div = require("elements.body").size,
    link = require("elements.text").size,
}

return ElementSize