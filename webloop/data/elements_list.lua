local ElementList = {
    body = require("elements.body").exec,
    text = require("elements.text").exec,
    div = require("elements.body").exec,
    link = require("elements.text").exec,
}

return ElementList